[alias]
xtask = "run --package xtask --"
test-all = "run --package xtask -- test-all"
test-system = "run --package xtask -- test-system"
test-leaks = "run --package xtask -- test-leaks"
lint = "run --package xtask -- lint"
